import type { FunctionExecuteResult } from "./function-execute";

/**
 * Placeholder for sibling “Desk: Settings Developer Hub core”.
 * Replace this export when Functions execute lands. Do not invent a second Functions table here.
 */
export async function executeDeskFunction(
  _apiName: string,
  _payload: Record<string, unknown>,
): Promise<FunctionExecuteResult | null> {
  return null;
}
